name: Build USB-Input directory for ESP32-S3

on:
  push:
    branches: [ "**" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repo
      uses: actions/checkout@v5
      with:
        submodules: 'recursive'
    - name: esp-idf build
      uses: espressif/esp-idf-ci-action@v1
      with:
        esp_idf_version: v5.5.4
        target: esp32s3
        path: 'usb-input'
    - name: Upload firmware artifacts
      uses: actions/upload-artifact@v4
      with:
        name: usb-input-bin
        retention-days: 7
        path: |
          usb-input/build/*.bin
          usb-input/build/*.elf
          usb-input/build/*.map
